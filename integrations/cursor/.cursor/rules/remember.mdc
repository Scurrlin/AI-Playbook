---
description: Use when the user asks to save session memory, restore memory, continue from memory, or invokes /rememberSave or /rememberRestore.
alwaysApply: false
---

Follow `skills/remember/SKILL.md`.

Never persist secrets. Save only the concise handoff context needed for a future session.
